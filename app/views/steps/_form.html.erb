<%= form_for(@step, namespace: local_assigns[:steps]) do |f| %>
  <% if @step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@step.errors.count, "error") %> prohibited this step from being saved:</h2>

      <ul>
      <% @step.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :steps_today %><br>
    <%= f.number_field :steps_today %>
  </div>
  <div class="field">
    <%= f.label :date_of_steps %><br>
    <%= f.text_field :date_of_steps, class: "datepicker"%>
  </div>
  <div class="actions">
    <center><%= f.submit "Record Today's Steps", class: "btn btn-default btn-primary" %></center>
  </div>
<% end %>
